##### gittalk hook (begin) #####
if [ ! -d "$GIT_DIR/rebase-merge" ]; then
export LANG="en_US.UTF-8"
# export PATH="<TODO add current path from --enable>:$PATH"
GITTALK_COMMIT_MSG=$1 gittalk --trigger >> $1
exit 2
fi
##### gittalk hook (end) #####